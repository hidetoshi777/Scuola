# Scuola

Dopo ogni nuova attività: apri una pull request e fai subito il **merge** su `main`.

GitHub è l’archivio del codice. Gli alunni aprono i file `.html` nel browser, non la pagina GitHub del file.
